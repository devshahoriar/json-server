const videos = require("./data/videos");
const tags = require("./data/tags");
const transactions = require("./data/db_transactions");
const todos = require("./data/db_todos");


module.exports = {
  todos,
  videos,
  tags,
  transactions,
}

